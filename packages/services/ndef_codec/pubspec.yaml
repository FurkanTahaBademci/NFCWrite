name: ndef_codec
description: >-
  NDEF mesaj/kayit kodlayici-cozucu ve tum kayit tipi olusturuculari
  (URI, Text, WiFi, vCard, Bluetooth, MIME, External...). Saf Dart, test edilebilir.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  meta: ^1.15.0
  nfc_core: ^0.1.0
  shared_utils: ^0.1.0

dev_dependencies:
  test: ^1.25.0
